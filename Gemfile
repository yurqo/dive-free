# Gemfile — App Store Connect delivery tooling only (Fastlane `deliver`).
# This is NOT part of the app build (Tuist/Xcode). It exists solely so
# localized App Store metadata + screenshots can be uploaded with:
#   bundle install && bundle exec fastlane metadata
source "https://rubygems.org"

gem "fastlane"
